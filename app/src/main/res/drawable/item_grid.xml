<!-- res/drawable/item_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 设置边框颜色和宽度 -->
    <stroke
        android:width="1dp"
        android:color="@color/color_fda341" />

    <!-- 设置填充颜色 -->
    <solid android:color="#fddec2" />

    <!-- 设置内边距 -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="8dp"
        android:bottom="8dp" />
</shape>